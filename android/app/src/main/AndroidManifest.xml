<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter only declares INTERNET in the debug and profile manifests, for
         hot reload. Without it here the app has no network at all in release:
         Ollama, bring-your-own-key cloud models and pack downloads would each
         fail on exactly the builds users install. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Lets the app hand a downloaded update to the package installer.
         Council is distributed as a direct APK download and not through Google
         Play, so nothing else can tell a reader a new build exists or install
         it for them. Requesting the install is all this grants: the reader
         still has to allow this app to install unknown apps, and still has to
         confirm in the system installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application
        android:label="Council"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Hands the downloaded APK to the package installer. A file:// URI
             throws FileUriExposedException on Nougat and later — which is every
             device this app runs on — so the installer has to be given a
             content:// URI from a provider instead.

             Its own authority rather than a plugin's: share_plus declares a
             FileProvider too, and two providers cannot claim one authority
             without the install failing at build time.

             Not exported, and it grants read access per-URI for the one intent
             that carries it, so nothing else on the device can read the app's
             files through it. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths" />
        </provider>

        <!-- Lets a downloaded model run on the GPU. Without these the OpenCL
             loader cannot dlopen the vendor driver on Android 12+, and the
             engine falls back to WebGPU — which is not merely slower: it
             hard-freezes some Mali GPUs. All optional, so a device with no
             OpenCL still installs and runs on the CPU. -->
        <uses-native-library android:name="libvndksupport.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL-car.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
